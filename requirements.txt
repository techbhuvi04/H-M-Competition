# GPU dataframes: install RAPIDS cuDF separately, see https://docs.rapids.ai/install
# e.g. pip install --extra-index-url=https://pypi.nvidia.com cudf-cu12
pandas>=1.3
numpy>=1.21
scikit-learn>=1.0
lightgbm>=3.3
jupyter
kaggle
